# Use the real database when false
USE_MOCK_DATA=false

# Password for the /admin upload page
ADMIN_PASSWORD=change-me

# MySQL connection string (separate database name recommended)
DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/lcwu_past_papers
DATABASE_SSL=true

# Setup: npm run db:setup && npm run db:seed-courses
